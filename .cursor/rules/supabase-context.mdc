---
description: Load Supabase schema, edge functions, and DB functions into context
globs: 
alwaysApply: true
---
---
description: Load Supabase schema, edge functions, and DB functions into context
mcpResources:
  - server: supabase
    type: supabase.database.schema
  - server: supabase
    type: supabase.edge.functions
  - server: supabase
    type: supabase.functions
alwaysApply: true
---


### Notify the user:
If all succeeded: ✅ Supabase schema, edge functions, and DB functions loaded.
If some failed: ⚠️ One or more Supabase resources could not be loaded. Check rule or connection.


{{
  if (!ctx.mcp.has("supabase.database.schema")) {
    print("⚠️ Could not load Supabase schema")
  }
  if (!ctx.mcp.has("supabase.edge.functions")) {
    print("⚠️ Could not load Supabase edge functions")
  }
  if (!ctx.mcp.has("supabase.functions")) {
    print("⚠️ Could not load Supabase DB functions")
  }
  print("✅ Supabase rule loaded")
}}

